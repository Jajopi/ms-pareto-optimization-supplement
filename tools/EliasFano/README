To compile, run `make`. It uses code in submodule '2i_bench', whose submodule 'succinct' requires library Boost >= 1.42.

To get EliasFano encoding, run `cat mask.txt | ./EliasFano >output.ef`, where 'mask.txt' contains a single line with a string of 0s and 1s and the resulting file is in a binary format.
